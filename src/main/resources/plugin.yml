name: RedstoneIndustry
version: '${version}'

main: net.craftoriya.redstoneIndustry.RedstoneIndustry
api-version: '26.1.2'

dependencies:
  server:
    AdaptersLib:
      load: BEFORE
      required: true